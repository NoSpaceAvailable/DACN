{
  "routes": [
    {"method": "POST", "path": "/login", "notes": "takes username and password fields"}
  ],
  "observations": [
    "Login fails for invalid credentials.",
    "Source inspection is available in this fixture.",
    "Error handling is generic; exploitability must be reasoned from code evidence."
  ]
}
