{
  "routes": [
    {"method": "GET", "path": "/", "notes": "landing page with link to /poems/"},
    {"method": "GET", "path": "/poems/", "notes": "form with buttons for poem1.txt, poem2.txt, poem3.txt"},
    {"method": "GET", "path": "/poems/?poem=poem1.txt", "notes": "displays the selected file content inside <pre> tags"}
  ],
  "observations": [
    "The poem parameter selects which file to display."
  ]
}
