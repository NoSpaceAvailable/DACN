{
  "routes": [
    {"method": "POST", "path": "/auth/login", "notes": "form fields username + password; returns an access_token JWT on success"},
    {"method": "POST", "path": "/auth/forgot_password", "notes": "form field email; marked 'Internal Only' in the source"},
    {"method": "POST", "path": "/auth/update_password", "notes": "form fields username + token + new_password; marked 'Internal Only' in the source"},
    {"method": "POST", "path": "/process/debug", "notes": "marked 'Admin Only'; requires a Bearer JWT"},
    {"method": "POST", "path": "/process/documents", "notes": "marked 'Admin Only'; requires a Bearer JWT; form field file_name; returns base64-encoded document contents"}
  ],
  "observations": [
    "All five routes above are exposed externally regardless of the 'Internal Only' / 'Admin Only' source comments.",
    "POST /process/documents serves files under the /cornhub directory; a flag file is known to be present on the server filesystem."
  ]
}
