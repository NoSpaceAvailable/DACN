{
  "routes": [
    {"method": "GET", "path": "/", "notes": "landing page with link to /poems/"},
    {"method": "GET", "path": "/poems/", "notes": "poem selector form"},
    {"method": "GET", "path": "/poems/?poem=poem1.txt", "notes": "returns poem file content inside a <pre> tag"}
  ],
  "observations": [
    "The poem parameter selects which file to display.",
    "Some inputs return HTTP 400 with body 'blocked: directory traversal not allowed'."
  ]
}
