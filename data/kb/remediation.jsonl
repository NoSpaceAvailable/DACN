{"id": "rem-idor", "attack_family": "IDOR", "severity_default": "High", "report_template": "Broken object-level authorization allows an authenticated user to access another tenant's object by changing an identifier. Impact includes data exposure and privilege boundary bypass."}
{"id": "rem-ssrf", "attack_family": "SSRF", "severity_default": "High", "report_template": "The server performs outbound requests to attacker-controlled URLs. Impact includes internal network reachability and cloud metadata exposure if egress rules are weak."}
{"id": "rem-sqli", "attack_family": "SQLi", "severity_default": "Critical", "report_template": "Unsafe SQL construction allows attacker input to modify query behavior. Impact ranges from data disclosure to authentication bypass or remote code execution depending on DB capabilities."}
