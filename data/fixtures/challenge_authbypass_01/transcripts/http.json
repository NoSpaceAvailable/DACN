{
  "routes": [
    {"method": "GET", "path": "/", "notes": "landing page mentioning the OAuth endpoints"},
    {"method": "GET", "path": "/login", "notes": "renders a login form"},
    {"method": "POST", "path": "/login", "notes": "accepts username + password and creates a session"},
    {"method": "POST", "path": "/oauth2/authorize", "notes": "OAuth2 authorize endpoint; returns a code"},
    {"method": "POST", "path": "/oauth2/token", "notes": "OAuth2 token endpoint; exchanges a code for a Bearer JWT"},
    {"method": "GET", "path": "/protected", "notes": "requires a Bearer JWT; returns the flag for authorised tokens"}
  ],
  "observations": [
    "All JWTs are signed with HS256.",
    "Login attempts with any non-empty username + password succeed and create a session."
  ]
}
