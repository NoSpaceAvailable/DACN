{
  "routes": [
    {"method": "POST", "path": "/api/login", "notes": "returns a bearer token"},
    {"method": "GET", "path": "/api/invoices/<invoice_id>", "notes": "returns the invoice as JSON; requires Authorization header"}
  ],
  "observations": [
    "Authenticated requests to /api/invoices/{id} return JSON containing the requester id and invoice fields.",
    "Invoice ids appear to be small integers."
  ]
}
