{
  "routes": [
    {"method": "POST", "path": "/api/login", "notes": "returns a bearer token"},
    {"method": "POST", "path": "/api/preview", "notes": "accepts JSON body {\"url\": ...}; fetches the URL and returns status, snippet"},
    {"method": "GET", "path": "/api/preview/history", "notes": "lists previously previewed links"}
  ],
  "observations": [
    "POST /api/preview with a public URL returns the response status code and a short body snippet."
  ]
}
