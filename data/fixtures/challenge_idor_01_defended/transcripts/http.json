{
  "routes": [
    {"method": "POST", "path": "/api/login", "notes": "returns bearer token"},
    {"method": "GET", "path": "/api/invoices?invoice_id=1001", "notes": "returns invoice JSON"},
    {"method": "GET", "path": "/api/invoices?invoice_id=1002", "notes": "returns 403 Forbidden"}
  ],
  "observations": [
    "The application exposes an invoice endpoint that takes invoice_id as a query parameter.",
    "Authenticated user 7 owns invoice 1001 but not 1002.",
    "Requests for invoices not owned by the authenticated user are rejected with 403."
  ]
}
