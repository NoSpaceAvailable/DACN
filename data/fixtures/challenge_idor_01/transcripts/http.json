{
  "routes": [
    {"method": "POST", "path": "/api/login", "notes": "returns bearer token"},
    {"method": "GET", "path": "/api/invoices/1001", "notes": "returns own invoice"},
    {"method": "GET", "path": "/api/invoices/1002", "notes": "returns invoice data even for other tenant when authenticated"}
  ],
  "observations": [
    "Authenticated requests to /api/invoices/{id} return JSON records.",
    "Changing the invoice id from 1001 to 1002 returns another customer's email and amount.",
    "No ownership check is visible in observed behavior."
  ]
}
