{
  "routes": [
    {"method": "GET", "path": "/", "notes": "landing page for the decryption service"},
    {"method": "POST", "path": "/decrypt", "notes": "decrypts data; requires a key_id header pointing at an existing key record"},
    {"method": "GET", "path": "/admin/key", "notes": "direct external requests are blocked by the reverse proxy"}
  ],
  "observations": [
    "Direct requests to /admin/* return 403 Forbidden.",
    "The application stack runs an internal Flask app behind a reverse proxy."
  ]
}
